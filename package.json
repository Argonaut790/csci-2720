{
	"name": "csci2720-project",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "concurrently --names 'backend,frontend' \"cd backend && yarn run start\" \"cd frontend && yarn run dev\" "
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Argonaut790/csci-2720.git"
	},
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
